# GitHub Actions - 生成小程序用的 JSON 数据
# ⚠️ 已废弃：此工作流已被拆分为两个独立工作流：
#   - deals-collect.yml (Workflow 1): 每天6点采集原始商品名单
#   - deals-enrich.yml (Workflow 2): 每小时补充价格并生成JSON
# 保留此文件但禁用调度，仅保留手动触发用于调试
name: 生成优惠数据（小程序）- 已废弃

on:
  # 调度已禁用，由 deals-collect.yml 和 deals-enrich.yml 替代
  # schedule:
  #   - cron: '0 23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  placeholder:
    name: 此工作流已废弃
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: 提示
        run: echo "此工作流已拆分为 deals-collect.yml 和 deals-enrich.yml，请使用新工作流。"
